{
  "name": "aether-battleground-server",
  "version": "1.0.0",
  "private": true,
  "description": "Persistent PvPvE 'Battleground' server for AETHER//PROTOCOL — runs the same host-authoritative sim as the browser co-op host, headlessly, over WebSockets, so the session survives no matter who's connected.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
